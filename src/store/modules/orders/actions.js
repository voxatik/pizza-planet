const actions = {
    async placeOrder({ commit }, order) {
        //todo store order in firebase db
        try {
            //todo
        } catch(error) {
            //todo
        }
    }
}


export default actions